import React, { FC } from 'react';

const Dashboard: FC = () => {
  return (
    <div className="container" data-tid="container">
      <h2>Dashboard</h2>
    </div>
  );
};

export default Dashboard;
